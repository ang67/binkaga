apiVersion: apps/v1      
kind: Deployment
metadata:
  labels:
    app: binkaga
  name: binkaga
  namespace: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: binkaga
  strategy: {}
  template:
    metadata:
      labels:
        app: binkaga
    spec:
      containers:
      - image: binkaga:1.0
        name: binkaga
        ports:
        - containerPort: 3000
        resources:
          limits: 
            cpu: 300m
            memory: 1000Mi
          requests: 
            cpu: 10m
            memory: 100Mi
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: binkaga
  name: binkaga
spec:
  type: ClusterIP
  ports:
  - port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app: binkaga

---

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: demo-localhost
spec:
  ingressClassName: nginx
  rules:
  - host: demo.localdev.me
    http:
      paths:
      - backend:
          service:
            name: binkaga
            port:
              number: 3000
        path: /
        pathType: Prefix